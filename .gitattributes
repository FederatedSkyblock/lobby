# Line endings for the Gradle wrapper scripts
/gradlew        text eol=lf
*.bat           text eol=crlf

# Versioned lobby world — stored via Git LFS (large binary, pulled by the docker image on boot)
world.zip       filter=lfs diff=lfs merge=lfs -text
